services:
  dashboard:
    build: .
    image: mcp-eval:local
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - MCP_EVAL_DB=/app/data/eval.duckdb
    volumes:
      - ./data:/app/data
    restart: unless-stopped
